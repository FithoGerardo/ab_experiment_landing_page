# ab_experiment_landing_page
An A/B test was run on the landing page, comparing two versions (A and B) with the goal of improving the conversion rate and revenue per user.  The company needs a data-driven decision to determine which version to implement, considering the conversion rate, average spend, and user behavior by traffic channel and user type.
